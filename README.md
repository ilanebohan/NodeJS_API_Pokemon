# NodeJS_Pokemon_API
 
DEPLOYING (on Heroku) :

git init
git add .
git commit -m "commit"
heroku create (in CMD if doesn't work into VSCODE terminal)

git push heroku master

to Open : heroku open





